1) Data sharding
2) Data replication
3) CDN -> Redis Cache -> Database
4) Pre Signed URLS (used by instagrams)
5) Proxy vs Reverse Proxy vs load balancing
5) JWT. is stateless. vs session. JWT is a random string. JWT is Public key cryptography mechanism.
    JWT is structured by using three parts -> header , information , verifiable signature.
    JWT can be used for authentication , authorizaton or information exchange.
    JWT can be validated by using refresh token or by using expiry data . So , most of the times JWT is short lived.
6)Stateless vs Stateful mechanism.
7)Authentication vs Authorization.
8)CORS
9)Local storage vs session storage vs cookies.
